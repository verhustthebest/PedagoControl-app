import type{ReactNode}from'react';export function ParentState({loading,error,empty,children,retry}:{loading:boolean;error?:string;empty?:boolean;children:ReactNode;retry?:()=>void}){if(loading)return <div className="parent-card parent-loading" role="status">Chargement sécurisé…</div>;if(error)return <div className="parent-card parent-error"><p>{error}</p>{retry&&<button className="parent-button" onClick={retry}>Réessayer</button>}</div>;if(empty)return <div className="parent-card parent-empty">Aucune donnée disponible.</div>;return <>{children}</>}
export function ParentHead({icon,title,subtitle}:{icon:string;title:string;subtitle:string}){return <header className="parent-head"><div className="parent-head-icon">{icon}</div><div><h1>{title}</h1><p>{subtitle}</p></div></header>}
