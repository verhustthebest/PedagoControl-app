import{useEffect,useMemo,useState}from'react';import{useParams}from'react-router-dom';import{ParentHead,ParentState}from'../../components/parent/ParentState';import{apiErrorMessage}from'../../services/api';import{assertPastOrToday,parentApi}from'../../services/parentApi';import type{ParentJournal}from'../../types/parent';
export function ParentJournalDetail(){const{publicId,date}=useParams();const[journal,setJournal]=useState<ParentJournal|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[comment,setComment]=useState('');const[saving,setSaving]=useState(false);const lessons=useMemo(()=>journal?.lessons.filter(item=>item.validation_status==='validated')||[],[journal]);const load=()=>{if(!publicId||!date)return;setLoading(true);setError('');try{assertPastOrToday(date)}catch(e){setError(apiErrorMessage(e));setLoading(false);return}void parentApi.journal(publicId,date).then(setJournal).catch(e=>setError(apiErrorMessage(e))).finally(()=>setLoading(false))};useEffect(load,[publicId,date]);const acknowledge=async()=>{if(!publicId||!date||saving||!lessons.length||journal?.acknowledgement)return;if(!window.confirm('Confirmer le visa de ce journal ? Cette action est enregistrée.'))return;setSaving(true);setError('');try{const result=await parentApi.acknowledge(publicId,date,comment);setJournal(current=>current?{...current,acknowledgement:result.acknowledgement}:current)}catch(e){setError(apiErrorMessage(e))}finally{setSaving(false)}};return <><ParentHead icon="✓" title="Journal quotidien" subtitle={date?`Cours validés du ${new Date(`${date}T12:00:00`).toLocaleDateString('fr-FR')}`:'Date indisponible'}/><ParentState loading={loading} error={error} empty={!journal} retry={load}>{journal&&<><section className="parent-card parent-lessons">{lessons.length?lessons.map((lesson,index)=><article className="parent-lesson" key={`${lesson.subject}-${index}`}><div><strong>{lesson.start_time||'Horaire indisponible'}</strong></div><div><h3>{lesson.subject}</h3><span>{lesson.teacher.first_name} {lesson.teacher.last_name}</span></div><div><strong>Résumé</strong><p>{lesson.summary||'Aucun résumé communiqué.'}</p>{lesson.homework&&<><strong>Travail à domicile</strong><p>{lesson.homework}</p></>}</div><div><span className="parent-status">Validé</span>{lesson.observations&&<p>{lesson.observations}</p>}</div></article>):<div className="parent-empty">Aucun cours validé pour cette date.</div>}</section><section className="parent-card parent-visa"><h2>Visa parental journalier</h2>{journal.acknowledgement?<div className="parent-success">Journal visé le {new Date(journal.acknowledgement.acknowledged_at).toLocaleString('fr-FR')} — {journal.acknowledgement.lesson_count_snapshot} cours enregistrés.</div>:<><p>Votre visa confirme la consultation des {lessons.length} cours actuellement autorisés.</p><textarea className="parent-comment" maxLength={500} value={comment} onChange={e=>setComment(e.target.value)} placeholder="Commentaire facultatif (texte uniquement)"/><div className="parent-actions"><button className="parent-button" disabled={!lessons.length||saving} onClick={acknowledge}>{saving?'Enregistrement…':'Confirmer le visa'}</button></div></>}</section></>}</ParentState></>}
