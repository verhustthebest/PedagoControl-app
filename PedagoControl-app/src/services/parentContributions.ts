import{apiRequest}from'./api';export type ContributionMode='PRIS_EN_CHARGE_PAR_ECOLE'|'CONTRIBUTION_PARENT';export type ContributionSetting={public_id:string;mode:ContributionMode;monthly_amount:string;currency:string;due_day:number;grace_days:number;reminder_days:number[];updated_at:string};export type ContributionPayment={public_id:string;amount:string;currency:string;payment_method:string;reference?:string|null;notes?:string|null;paid_at:string};export type ContributionDue={public_id:string;period_start:string;period_end:string;due_date:string;mode:ContributionMode;amount_due:string;amount_paid:string;balance:string;currency:string;status:'PRIS_EN_CHARGE'|'EN_REGLE'|'A_RENOUVELER'|'PARTIEL'|'EN_RETARD'|'SUSPENDU';days_remaining:number;student:{public_id:string;first_name:string;last_name:string;matricule?:string};payments:ContributionPayment[]};export type ContributionPage={dues:ContributionDue[];pagination:{page:number;limit:number;total:number;total_pages:number}};const qs=(f:Record<string,string|number|undefined>)=>{const p=new URLSearchParams();Object.entries(f).forEach(([k,v])=>{if(v!==undefined&&v!=='')p.set(k,String(v))});return p.toString()};
/** Toutes les opérations école restent scellées au school_id de la session et aux public_id des échéances. */
export const contributionApi={setting:(school:string)=>apiRequest<{setting:ContributionSetting|null}>(`/parental/schools/${encodeURIComponent(school)}/contribution-settings`),saveSetting:(school:string,body:Omit<ContributionSetting,'public_id'|'updated_at'>)=>apiRequest<{setting:ContributionSetting}>(`/parental/schools/${encodeURIComponent(school)}/contribution-settings`,{method:'PUT',body:JSON.stringify(body)}),generate:(school:string,period:string)=>apiRequest<{created:number}>(`/parental/schools/${encodeURIComponent(school)}/contribution-dues/generate`,{method:'POST',body:JSON.stringify({period})}),list:(school:string,f:Record<string,string|number|undefined>)=>apiRequest<ContributionPage>(`/parental/schools/${encodeURIComponent(school)}/contribution-dues?${qs(f)}`),detail:(school:string,id:string)=>apiRequest<{due:ContributionDue}>(`/parental/schools/${encodeURIComponent(school)}/contribution-dues/${encodeURIComponent(id)}`),pay:(school:string,id:string,body:{amount:string;payment_method:string;reference?:string;notes?:string})=>apiRequest<{payment:ContributionPayment}>(`/parental/schools/${encodeURIComponent(school)}/contribution-dues/${encodeURIComponent(id)}/payments`,{method:'POST',body:JSON.stringify(body)}),history:(school:string,id:string)=>apiRequest<{payments:ContributionPayment[]}>(`/parental/schools/${encodeURIComponent(school)}/contribution-dues/${encodeURIComponent(id)}/payments`),own:()=>apiRequest<{dues:ContributionDue[];summary:{balance:string;next_due_date:string|null}}>('/parental/me/contributions')};
