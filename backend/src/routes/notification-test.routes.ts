import{Router,type NextFunction,type Response}from'express';import{notificationTestConfig,sendTest}from'../controllers/notification-test.controller';import{authenticateBearerToken,requireRole}from'../middleware/auth.middleware';import{notificationTestRateLimit}from'../middleware/rate-limit.middleware';import type{AuthenticatedRequest}from'../middleware/auth.middleware';import{validate}from'../middleware/validate.middleware';import{notificationTestBody}from'../validation/schemas'
const router=Router();const disabledInProduction=(_request:AuthenticatedRequest,response:Response,next:NextFunction)=>process.env.NODE_ENV==='production'?response.status(404).json({message:'Resource not found'}):next();const access=[disabledInProduction,authenticateBearerToken,requireRole('SUPER_ADMIN')]as const;
router.get('/notifications/tests/config',...access,notificationTestConfig);router.post('/notifications/tests/send',...access,notificationTestRateLimit,validate({body:notificationTestBody}),sendTest);export default router
