import{Router}from'express';import{listSchoolClasses}from'../controllers/school-class.controller';import{authenticateBearerToken,requirePermission,requireSchoolScope}from'../middleware/auth.middleware';import{validate}from'../middleware/validate.middleware';import{classListQuery,schoolParams}from'../validation/schemas';const router=Router();router.get('/schools/:schoolId/classes',authenticateBearerToken,requireSchoolScope(),requirePermission('VIEW_SCHOOL_CLASSES'),validate({params:schoolParams,query:classListQuery}),listSchoolClasses);export default router
